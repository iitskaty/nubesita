# Copia este archivo como .env y completa tus credenciales de MongoDB Atlas
# Atlas → Connect → Drivers → copia la URI de conexión
MONGODB_URI=mongodb+srv://<usuario>:<password>@<tu-cluster>.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=nubesita
PORT=3000
